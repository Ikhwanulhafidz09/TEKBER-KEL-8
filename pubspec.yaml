name: myits_sarprass
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Library tambahan
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.10.3
  url_launcher: ^6.2.5
  file_picker: ^8.0.0
  youtube_player_flutter: ^9.0.1
  
  # CATATAN: Baris 'assets' yang error tadi sudah saya hapus dari sini.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# Bagian Konfigurasi Flutter
flutter:
  uses-material-design: true

  # DAFTARKAN ASSETS DISINI (Indentasinya harus pas)
  assets:
    - assets/images/